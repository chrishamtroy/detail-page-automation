[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["integration: tests requiring real API keys (skipped in CI)"]

[tool.mypy]
python_version = "3.13"
ignore_missing_imports = true
warn_return_any = false
warn_unused_configs = true
